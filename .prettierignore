dist
coverage
CHANGELOG.md
package-lock.json

# 52,000 files of deliberately invalid JavaScript, cloned from somebody else's
# repository. `eslint.config.js` ignores it for the same reason.
test262/

# Source strings quoted exactly as a reference parser tests them. Reformatting
# one changes what is being tested.
packages/jskit/tests/parse/fixtures/
packages/jskit/tests/scope/fixtures/

# Generated by `astro build`. Formatting build output is churn, and it would
# make `fmt:check` depend on whether the app happens to have been built.
packages/jskit-inspect/.astro/

# Cargo's build directory: machine-generated, enormous, and not ours.
packages/jskit-native/target/
