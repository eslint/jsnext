[package]
name = "jskit-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Native core of @eslint/jskit: parser, scope analyzer, and control flow analyzer producing the jskit binary formats."

[dependencies]
unicode-id-start = "1"
