[
	"interface A { x: number; }",
	"interface A<T> { x: T; }",
	"interface A extends B, C<D> { m(a: E): F; }",
	"type A = B;",
	"type A<T> = T extends string ? T : never;",
	"type A<T> = T extends Array<infer U> ? U : never;",
	"type A = { [K in keyof B]: C };",
	"type A = { readonly [K in keyof B as D<K>]?: C };",
	"type A = (a: B, ...rest: C[]) => D;",
	"type A = new (a: B) => C;",
	"type A = { (a: B): C; new (d: E): F; [key: string]: G; m(h: I): J; p: K };",
	"type A = typeof b;",
	"type A = typeof b.c.d;",
	"type A = B['c'];",
	"type A = keyof B;",
	"type A = readonly B[];",
	"type A = [first: B, second?: C, ...rest: D[]];",
	"type A = B | C & D;",
	"type A = `prefix-${B}`;",
	"type A = import('m').B;",
	"const a: B = c;",
	"let a: B<C>;",
	"function f<T extends B>(a: T): T { return a; }",
	"function f(a: B): a is C { return true; }",
	"function f(this: A, b: B) {}",
	"const f = <T,>(a: T): T => a;",
	"declare function f(a: B): C;",
	"declare const a: B;",
	"declare global { interface Window { a: B } }",
	"namespace N { export const a = 1; }",
	"namespace N.M { export const a = 1; }",
	"module 'm' { export const a: B; }",
	"enum E { A, B = 2, C = D }",
	"const enum E { A = 'a', 'b c' = 'd' }",
	"enum E { A = 1 } const x = E.A;",
	"import a = require('m'); a;",
	"import A = B.C.D; A;",
	"export = a;",
	"import type { A } from 'm'; type B = A;",
	"import { type A, b } from 'm'; type C = A; b;",
	"const a = 1; export type { a };",
	"type A = B; export { type A };",
	"class A implements B, C<D> { }",
	"abstract class A { abstract m(a: B): C; abstract x: D; }",
	"class A { constructor(private readonly x: B, public y: C) {} }",
	"class A<T> extends B<T> { m<U>(a: U): T { return a as unknown as T; } }",
	"class A { declare x: B; m?(): void; }",
	"const a = b as C;",
	"const a = <C>b;",
	"const a = b satisfies C;",
	"const a = b!;",
	"b! = c;",
	"const a = f<B>;",
	"const a = f<B>(c);",
	"const a = new B<C>(d);",
	"function f(a?: B, b: C = d) {}",
	"type A = { a: B } & { c: D };",
	"let a: unique symbol;",
	"type A = abstract new () => B;",
	"function f(): asserts a is B {}",
	"type A<in out T> = T;",
	"type A = B extends C ? D extends E ? F : G : H;",
	"type Fn = <T>(a: T) => T;",
	"interface A { <T>(a: T): T }",
	"type A = { [K in B]: { [L in C]: K } };",
	"type A<T> = T extends (infer U)[] ? U : T extends (...args: infer V) => unknown ? V : never;",
	"@decorator class A { @member m() {} @prop x = 1; m2(@param a: B) {} }",
	"function f(a: number): void; function f(a: string): void; function f(a: unknown): void {}",
	"class A { static readonly x: B = c; }",
	"type A = B.C.D<E>;",
	"let a: this;",
	"export declare const a: B;",
	"export abstract class A {}",
	"using a = b;",
	"await using a = b;",
	"class A { accessor x = 1; }",
	"class A { static accessor x: B = c; }"
]
